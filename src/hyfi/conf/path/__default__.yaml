task_name: ${oc.select:..project.task_name, default-task}
task_root: ${oc.select:..project.project_data_root,${oc.env:HYFI_PROJECT_DATA_ROOT,.}}/${.task_name}

output_dir: ${.task_root}/outputs
data_dir: ${.task_root}/data
library_dir: ${.task_root}/libs
model_dir: ${.task_root}/models
cache_dir: ${oc.select:..project.path.project_cache,${.task_root}/cache}
tmp_dir: ${oc.select:..project.path.project_tmp,${.task_root}/tmp}
log_dir: ${oc.select:..project.path.project_logs,${.task_root}/logs}
