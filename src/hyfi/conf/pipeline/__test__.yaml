defaults:
  - __init__
  - /pipe@pipe1: load_dataframes
  - /pipe@pipe2: test_preprocessing
  - /pipe@pipe3: save_dataframes

steps:
  - pipe1
  - uses: pipe2
    verbose: true
  - uses: pipe3
    run:
      with:
        data_file: workspace/processed/ESG_ratings_raw.parquet
      verbose: true
    verbose: true
