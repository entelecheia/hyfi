# @package _global_
defaults:
  - __init__

debug_mode: true
print_config: true
verbose: true
ignore_warnings: false
logging_level: DEBUG
hydra:
  verbose: false
  job_logging:
    root:
      level: ${logging_level}
